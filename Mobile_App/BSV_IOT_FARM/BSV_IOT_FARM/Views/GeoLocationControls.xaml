<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BSV_IOT_FARM.Views.GeoLocationControls"
             Title="Geo Location Controls">
    <ContentPage.Content>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Frame CornerRadius="20" BorderColor="Black">
                <StackLayout>
                    <Label Text="Buzzer" HorizontalTextAlignment="Center" FontSize="Medium"/>
                    <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                        <Switch x:Name="buzzer" IsToggled="{Binding BuzzerIsOn}" HorizontalOptions="EndAndExpand" />
                    </StackLayout>
                </StackLayout>
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage>